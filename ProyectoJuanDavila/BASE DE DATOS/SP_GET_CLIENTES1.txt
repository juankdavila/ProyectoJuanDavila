-- FUNCTION: public.SP_GET_CLIENTES()

-- DROP FUNCTION IF EXISTS public."SP_GET_CLIENTES"();

CREATE OR REPLACE FUNCTION public."SP_GET_CLIENTES"(
	)
    RETURNS void
    LANGUAGE 'plpgsql'
    COST 100
    VOLATILE PARALLEL UNSAFE
AS $BODY$
BEGIN
	
	SELECT * FROM CLIENTES;

END;
$BODY$;

ALTER FUNCTION public."SP_GET_CLIENTES"()
    OWNER TO postgres;
