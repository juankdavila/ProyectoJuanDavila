-- FUNCTION: public.SP_GET_USUARIOS()

-- DROP FUNCTION IF EXISTS public."SP_GET_USUARIOS"();

CREATE OR REPLACE FUNCTION public."SP_GET_USUARIOS"(
	)
    RETURNS SETOF "USUARIOS" 
    LANGUAGE 'plpgsql'
    COST 100
    VOLATILE PARALLEL UNSAFE
    ROWS 1000

AS $BODY$

BEGIN
    SELECT * FROM "USUARIOS";
END;
$BODY$;

ALTER FUNCTION public."SP_GET_USUARIOS"()
    OWNER TO postgres;
